services:
  flux-pool:
    build: .
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      ACCESS_KEY: ""
      DATA_DIR: /data
    volumes:
      - flux-data:/data
volumes:
  flux-data:
